# Neon Postgres (Pro) — add when wiring the waitlist to the DB
DATABASE_URL="postgres://..."

# Resend — add when sending waitlist confirmation emails
RESEND_API_KEY="re_..."
WAITLIST_FROM_EMAIL="Driving Instructors Plymouth <hello@drivinginstructorsplymouth.com>"
